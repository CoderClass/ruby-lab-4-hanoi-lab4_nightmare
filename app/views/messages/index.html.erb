<strong style="font-size: 40px;"><%= "Room #{@room.name}"%></strong>
<ul class="list-group" style="padding: 32px 0;">
	<%= render @messages %>
</ul>

<%= form_for [@room, Message.new] do |f| %>
	<%= f.text_field :content %>
	<%= f.submit "Send" %>
<% end %>
